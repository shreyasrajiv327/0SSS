#ifndef MYTYPES_H
#define MYTYPES_H

// sigint_handler is a function pointer to the handler which accepts 
// int signal no. as a parameter and returns nothing
typedef void (* SIGHANDLER)(int signum);

#endif
